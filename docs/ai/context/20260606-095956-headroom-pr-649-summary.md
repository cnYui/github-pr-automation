# headroom PR #649 收尾记录

## PR

- 上游仓库：`chopratejas/headroom`
- PR：https://github.com/chopratejas/headroom/pull/649
- 状态：open draft
- 分支：`cnYui:codex/sse-crlf-events` -> `chopratejas:main`
- commit：`c0dc6e71 fix(proxy): parse CRLF SSE event terminators`

## 改动

- `headroom/proxy/helpers.py`
  - SSE bytes parser 从只识别 `\n\n` 改为识别 `\n\n` 与 `\r\n\r\n`。
  - 完整事件解码后使用 `splitlines()` 处理 LF/CRLF 行尾。
  - 保留完整事件 invalid UTF-8 直接抛错的既有行为。
- `tests/test_sse_utf8_split.py`
  - 新增 CRLF 事件分隔符回归测试。

## 验证

- 红测：
  - `.venv/bin/pytest tests/test_sse_utf8_split.py::test_crlf_terminated_event_is_parsed -q`
  - 失败原因：`events == []`，证明当前实现未解析 CRLF 完整事件。
- 绿测：
  - `.venv/bin/pytest tests/test_sse_utf8_split.py::test_crlf_terminated_event_is_parsed -q`
  - 通过：1 passed。
- PR 前验证：
  - `.venv/bin/pytest tests/test_sse_utf8_split.py tests/test_streaming_usage_parser.py -q`
  - 通过：13 passed。
  - `.venv/bin/ruff check headroom/proxy/helpers.py tests/test_sse_utf8_split.py`
  - 通过：All checks passed。

## 环境备注

- `uv sync --extra dev` 因上游 `uv.lock` 中 `gitpython` wheel 版本不一致失败。
- 改用 Python 3.11 本地 `.venv` 执行 `pip install -e ".[dev]"` 后完成测试。
