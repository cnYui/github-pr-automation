# AGENTS.md 压缩归档（2026-09-07）

- 时间：2026-09-07 01:06:18 +09:00
- 触发：本机自动运行的仓库文档日常维护流水线（docs/ai/context 清理 + AGENTS.md 压缩）
- 压缩前：23 行（`wc -l`）/ 5504 字节
- 压缩后：22 行（`wc -l`）/ 5161 字节
- 移除条目：1 条

本仓库为公开仓：归档中若出现密钥/内网主机/桶名/Tunnel ID 等敏感值一律以 `${变量名}` 占位；本次移除内容不含此类敏感值，全部照抄原文。

## 一、被移除的条目（原文完整照抄）

### 1. 2026-06-07 graphiti CLA 签署操作日志

原文（逐字照抄）：

```
- 2026-06-07 graphiti CLA：用户确认已阅读并同意 Zep CLA 后，已用 `cnYui` 账号在 `getzep/graphiti#1539` 发送个人 CLA 签署评论（评论 `#issuecomment-4641187179`）；即时核对时 CLA check 仍停留在旧 failure，等待 bot 刷新，详见 `docs/ai/context/20260607-113201-graphiti-pr-1539-cla-signature.md`。
```

命中判据：**一次性的操作日志，且条目里已引用了对应的 record 文档**。该条记录的是一次已完成的具体操作（用 `cnYui` 账号在 `getzep/graphiti#1539` 发送个人 CLA 签署评论），完整细节保存在其引用的 record 文档 `docs/ai/context/20260607-113201-graphiti-pr-1539-cla-signature.md` 中；CLA 已为settled 历史（见 commit `1c2ec7e`「graphiti#1568/#1539 CLA 已签署」）。该条不确立任何至今仍生效的规则或坑，属可复现的历史流水，符合移除判据。应用「下一个会话读不到这条会做错什么？」测试：CLA 已签署完毕，读不到不会导致任何错误操作。

## 二、并入保留章节的事实

本次无。被移除的唯一条目不含需要并入其它章节的至今生效规则或坑。

## 三、本次刻意保留的内容

以下条目虽写法像历史记录，但确立了至今仍照做的规则、边界、坑或待办，一律保留：

- `# 项目记忆` 标题与整节 `## GitHub 每日 PR 机会展示页`（首版目标、页面/自动化/数据/贡献范围边界、推荐门槛、Skill 方向、实现计划引用）——按规则整节保留。
- **2026-06-07 失败 PR 根因复查**：确立坑「`graphiti#1539`/`CopilotKit#5296`/`cell-architecture-studio#8` 均不应通过改代码或空提交重提解决」，是可复用的教训，保留。
- **2026-07-11 候选实施复核修正**：确立规则「后续机会扫描必须同时检查默认分支实现状态，不能只看 issue/PR 状态」——反直觉且至今生效的坑，保留。
- **2026-07-11 主控仓库方向**：仓库改名 `github-pr-automation`、扫描/执行/编排三 Skill、一个 cron 串联、`skills/` 为唯一源码——架构决策，保留。
- **2026-07-11 自动执行授权**：`值得继续`→live preflight→允许 clone/fork/改/验证/commit/push/建 PR、**禁止自动 merge**、内部中文/上游随目标仓语言——授权与语言边界，保留。
- **2026-07-11 持久化边界**：每轮不可变候选快照、lease、current run、ledger；`work/`、`data/` 不进 Git——持久化设计，保留。
- **2026-07-11 GitHub 工具边界**：`gh` 负责认证/远程操作，本地改动用 Codex+工具链+`git`，始终禁止自动 merge——工具边界，保留。
- **2026-07-11 主控仓库迁移完成**：含至今生效的主控仓路径/remote 事实（GitHub `cnYui/github-pr-automation`、本地 `D:\CodeWorkSpace\github-pr-automation`），虽含一次性迁移流水但混有 keep-worthy 事实，整条保留（宁可少删）。
- **2026-07-14 主控仓同步边界**：同步范围与排除项、`origin` 已配置不重复创建——同步边界，保留。
- **2026-07-14 `destructive_command_guard` PR 阻塞**：该仓 LICENSE rider 排除 OpenAI/Anthropic 及其代理，未取得作者书面许可前不得推进——坑 + 约束，保留。
- **2026-09-06 待办合并**：扫描器两个已知未修复缺陷（CLI 报告 UTC 日期偏移、`--help` 未静默）——待办，保留。
