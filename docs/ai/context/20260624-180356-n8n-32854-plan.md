# n8n #32854 最小 PR 实施计划

## 目标

给 `n8n-io/n8n` 提交一个最小 PR，修复 MCP `document_processing` best-practices 中 `Extract from File` 默认输出 key 的错误示例，并补对应测试。

## 文件范围

- 修改：`packages/@n8n/workflow-sdk/src/prompts/best-practices/guides/document-processing.ts`
  - 修正文案中的默认输出 key
  - 增加对 `destinationKey` 默认值的简短说明
- 修改：`packages/cli/src/modules/mcp/__tests__/get-workflow-best-practices.tool.test.ts`
  - 为 `document_processing` 增加定向断言

## 步骤

1. 准备目标仓隔离工作目录或 worktree
   - 优先使用隔离工作区，避免污染当前已有目录

2. 拉取 `n8n-io/n8n` 并创建分支
   - 分支名建议：`codex/fix-mcp-document-processing-output-key`

3. 先读目标文件，确认错误文案的真实上下文
   - 找到 `Extract from File` 段落
   - 确认当前写法确实是 `$json.text`

4. 先读目标测试文件，确认适合补哪种断言
   - 优先补局部字符串断言
   - 避免无必要的大快照更新

5. 先写或先补测试断言
   - 针对 `document_processing` best-practices 输出
   - 断言包含 `$json.data`
   - 断言不包含错误默认示例

6. 运行目标测试，确认旧文案下测试先失败

7. 最小修改文案
   - 把默认访问示例改为 `{{ $json.data }}`
   - 补充一句 `destinationKey` 可覆盖默认值

8. 再跑目标测试，确认通过

9. 做低成本收尾验证
   - `git diff --check`
   - 必要时全文搜索错误文案，确认目标文件中已消失

10. 提交、推送、创建 PR
    - commit 信息聚焦 MCP best-practices 文案修复
    - PR 描述引用 `Fixes #32854`
    - 明确说明这次只修 guidance，不改 runtime 行为

## 验证命令

优先预期命令：

```powershell
pnpm test packages/cli/src/modules/mcp/__tests__/get-workflow-best-practices.tool.test.ts
git diff --check
rg -n '\$json\.text|\$json\.data|destinationKey' packages/@n8n/workflow-sdk/src/prompts/best-practices/guides/document-processing.ts
```

如果仓内测试脚本需要调整，则以目标测试文件的实际可运行命令为准，但不扩大验证范围到整仓全量测试。

## 收口标准

- 文案已改成 `{{ $json.data }}`
- 测试能覆盖这次修复
- 没有额外无关改动
- PR 可以直接指向 `#32854`
