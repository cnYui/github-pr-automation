# n8n #32854 提交记录

时间：2026-06-25 10:46:03 JST

## 结论

已提交 draft PR：`https://github.com/n8n-io/n8n/pull/32983`

## 改动

- 将 `document_processing` 的 Extract from File 指南从 `{{ $json.text }}` 修正为默认 `{{ $json.data }}`
- 补充 `destinationKey` 说明，避免把可配置输出键写死成 `text`
- 增加一条 MCP 单测，直接锁定这段文案

## 验证

- `pnpm run test:win -- src/modules/mcp/__tests__/get-workflow-best-practices.tool.test.ts`
- `pnpm run lint` in `packages/@n8n/workflow-sdk`
- `pnpm run typecheck` in `packages/@n8n/workflow-sdk`
- `NODE_OPTIONS=--max-old-space-size=8192 pnpm run lint` in `packages/cli`
- `pnpm run test:win -- src/modules/mcp/__tests__/get-workflow-best-practices.tool.test.ts` in `packages/cli`

## 备注

- `packages/cli` 的宽 `typecheck` 仍有与本次改动无关的跨包报错，集中在 Azure binary-data、RoleRepository 和 task-runner 共享类型
- PR 当前为 draft，base 为 `master`
